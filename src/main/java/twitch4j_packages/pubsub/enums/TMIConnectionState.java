package twitch4j_packages.pubsub.enums;

/**
 * WebSocket - Connection State
 */
public enum TMIConnectionState {
    DISCONNECTING,
    RECONNECTING,
    DISCONNECTED,
    CONNECTING,
    CONNECTED
}
